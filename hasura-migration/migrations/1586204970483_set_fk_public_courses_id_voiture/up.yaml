- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "public"."courses"
                 add constraint "courses_id_voiture_fkey"
                 foreign key ("id_voiture")
                 references "public"."voiture"
                 ("id") on update restrict on delete restrict;
  type: run_sql
