- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "armadacar"."voitures"
                 add constraint "voitures_id_entreprise_fkey"
                 foreign key ("id_entreprise")
                 references "armadacar"."entreprises"
                 ("id") on update restrict on delete restrict;
  type: run_sql
