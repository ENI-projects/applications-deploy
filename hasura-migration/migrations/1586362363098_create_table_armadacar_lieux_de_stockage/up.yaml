- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "armadacar"."lieux_de_stockage"("id" serial NOT NULL, "libelle"
      text NOT NULL, "id_entreprise" integer NOT NULL, "adresse" text NOT NULL, "ville"
      text NOT NULL, "departement" text NOT NULL, "code_postal" text NOT NULL, "created_at"
      timestamptz NOT NULL DEFAULT now(), PRIMARY KEY ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: lieux_de_stockage
    schema: armadacar
  type: add_existing_table_or_view
