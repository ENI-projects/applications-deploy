- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "armadacar"."utilisateurs"("id" text NOT NULL, "id_entreprise"
      integer NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY
      ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: utilisateurs
    schema: armadacar
  type: add_existing_table_or_view
