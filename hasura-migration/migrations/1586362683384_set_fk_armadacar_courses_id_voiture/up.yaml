- args:
    cascade: false
    read_only: false
    sql: |-
      alter table "armadacar"."courses"
                 add constraint "courses_id_voiture_fkey"
                 foreign key ("id_voiture")
                 references "armadacar"."voitures"
                 ("id") on update restrict on delete restrict;
  type: run_sql
