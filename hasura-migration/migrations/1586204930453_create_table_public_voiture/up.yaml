- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."voiture"("id" serial NOT NULL, "marque" text NOT NULL,
      "modele" text NOT NULL, "nombre_de_place" integer NOT NULL, "immatriculation"
      text NOT NULL, "energie" text NOT NULL, "id_lieux_de_stockage" integer NOT NULL,
      "created_at" timestamptz NOT NULL DEFAULT now(), "id_entreprise" integer NOT
      NULL, PRIMARY KEY ("id") , FOREIGN KEY ("id_lieux_de_stockage") REFERENCES "public"."lieux_de_stockage"("id")
      ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("id_entreprise") REFERENCES
      "public"."entreprises"("id") ON UPDATE restrict ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: voiture
    schema: public
  type: add_existing_table_or_view
