- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."entreprises"("id" serial NOT NULL, "departement" text
      NOT NULL, "ville" text NOT NULL, "codePostal" text NOT NULL, "adresse" text
      NOT NULL, "responsable" text NOT NULL, PRIMARY KEY ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: entreprises
    schema: public
  type: add_existing_table_or_view
