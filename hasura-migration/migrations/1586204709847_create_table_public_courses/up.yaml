- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."courses"("id" serial NOT NULL, "date_debut" timestamptz
      NOT NULL, "date_fin" timestamptz NOT NULL, "lieu_depart" text NOT NULL, "lieu_arrivee"
      text NOT NULL, "remarque" text, "liste_passagers" text NOT NULL, "created_at"
      timestamptz NOT NULL DEFAULT now(), "id_voiture" integer NOT NULL, PRIMARY KEY
      ("id") , UNIQUE ("id"));
  type: run_sql
- args:
    name: courses
    schema: public
  type: add_existing_table_or_view
