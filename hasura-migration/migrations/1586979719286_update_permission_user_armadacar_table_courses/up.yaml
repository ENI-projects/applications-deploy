- args:
    permission:
      filter:
        utilisateurs_courses:
          _and:
          - createur:
              _eq: true
          - id_utilisateur:
              _eq: X-Hasura-User-Id
    role: user
    table:
      name: courses
      schema: armadacar
  type: create_delete_permission
