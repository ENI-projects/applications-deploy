- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."utilisateurs"("id" serial NOT NULL, "id_entreprise"
      integer NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), PRIMARY KEY
      ("id") , FOREIGN KEY ("id_entreprise") REFERENCES "public"."entreprises"("id")
      ON UPDATE cascade ON DELETE cascade, UNIQUE ("id"));
  type: run_sql
- args:
    name: utilisateurs
    schema: public
  type: add_existing_table_or_view
